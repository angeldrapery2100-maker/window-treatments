# OPUS 任务书 — 推广系统 P1：网站落地页 / Rewards / Partner 页 + 归因直通 + AI 估价 ×1.05

日期：2026-08-22　出自：Fable（企划书 `AAPP/推广系统企划-Referral-AI-Quote-Platform-2026-08-22.md`，以企划书为准）
仓库：`/Volumes/SSD2T/Projects/window-treatments`（`apps/web`）　执行：Opus　测试：Sonnet（附件 `SONNET-测试清单-推广系统P0P1-2026-08-22.md`）
接口契约：见 P0 任务书 §0（Firestore 形状 + 5 个 HTTP CF）。P0 未部署前用 `AAPP_REFERRAL_MOCK=1` 环境变量走本地 mock（§1.1）。

> 开工前必读：`AGENTS.md`、`docs/NEXT-STEPS-2026-07-05.md`、`docs/网站-AAPP对接总体蓝图-2026-08-11.md §四·六`、`app/c/[slug]/route.ts`（整份是模板）、`lib/campaigns.ts`、`lib/aappIntake.ts`、`lib/assistantTools.ts:250-256, 494-548, 912-935, 1571-1632`、`app/api/store/assistant/route.ts:440-450, 555-565`、`components/StoreAssistant.tsx:480-510`、`app/layout.tsx:53-105`、`lib/leadScoring.ts:19-33, 98-111`。
> 军规：定价目录零改动（`packages/shared/src/pricing/aapp/`、`lib/productPricing.ts`、`api/store/pricing/calculate`）；AI 知识文件不放价格；`websiteInquiry` 只加可选字段；沙箱跑不了 `next build`，验证 = `cd apps/web && ../../node_modules/.bin/tsc --noEmit` 且 `cd packages/shared && npx vitest run`。

---

## 1. 基础层

### 1.1 `lib/referral.ts`（新）

```ts
export const REFERRAL_COOKIE = 'ad_ref'
export const REFERRAL_COOKIE_MAX_AGE = 60 * 60 * 24 * 90
export const TOKEN_RE = /^[A-Za-z0-9_-]{16,32}$/
export type ReferralPublic = { referrerType: 'customer'|'agent'|'designer'|'contractor'|'company'|'campaign'; displayName: string; discountPct: number|null; tierLabel?: string; tierLabelCn?: string; referralCode?: string|null; active: boolean }
export function getReferralFromRequest(request: Request): string | null   // 照 campaigns.ts:20-24 正则抠 cookie
export async function lookupReferral(token: string): Promise<ReferralPublic | null>  // GET `${AAPP_REFERRAL_BASE}/referralLookup?t=`；5s 超时；非 ok → null；内存 LRU 60s
export async function recordReferralVisit(token: string, page: string, ua: string): Promise<void>  // POST referralVisit，fire-and-forget
export async function fetchReferralPortal(token: string): Promise<PortalView | null>      // GET referralPortal，no-store
export async function setReferralPortalPrefs(token: string, smsOptIn: boolean): Promise<{ok:boolean; smsOptIn?:boolean}>
export async function uploadPartnerW9(token: string, fileBase64: string, mime: string): Promise<{ok:boolean; error?:string}>
```

`AAPP_REFERRAL_BASE` 默认 `https://us-central1-angel-drapery.cloudfunctions.net`。所有请求带 `x-ad-key: process.env.AAPP_WEBINTAKE_SECRET`（若设置）。`AAPP_REFERRAL_MOCK=1` 时 `lookupReferral` 返回固定 fixture（三种 type 各一个 token：`mock-customer-0000000000`、`mock-agent-00000000000`、`mock-campaign-000000000`），`fetchReferralPortal` 同理——fixture 放 `lib/referral.mock.ts`。

### 1.2 `/r/[token]/page.tsx`（新，Server Component）

- `generateMetadata({params})`：token 合法且 lookup 命中 →
  `title: '{displayName} invited you — Free curtain AI consultant'`（customer/partner）/ `'Free curtain AI consultant'`（company/campaign）；
  `description: 'Measure your windows and get an instant curtain estimate. Free in-home measure in Los Angeles.'`；
  `openGraph: { title, description, images:[{url:'/og-referral.jpg', width:1200, height:630}], url:`/r/${token}` }`；`twitter: summary_large_image`；`robots: { index:false, follow:false }`；`alternates: { canonical: `/r/${token}` }`。
  lookup 未命中 → `redirect('/')`。
- 页面内容（Server 部分渲染静态，Client 岛 `ReferralLanding.tsx` 处理交互）：
  1. Logo + 标题「Measure your windows and get an instant curtain estimate」/「量一量窗户，马上拿到窗帘参考价」。
  2. 推荐人行：customer → 「{displayName} sent you this — friends get {discountPct}% off your first project」；partner → 「Referred by {displayName} · partner pricing applied」（**不显示百分比**，企划 §6-2）；company/campaign → 不显示此行。
  3. 三步卡：① Measure（链接 `/how-to-measure` 与 `/measure-wizard`）② Ask AI（按钮）③ Free in-home measure（按钮 → 打开 AI 并预填「I'd like to book a free in-home measure」）。
  4. 「Open in ChatGPT」按钮：渲染但 `disabled` + 「Coming soon」（P3 客户版 GPT 未上线；`NEXT_PUBLIC_CUSTOMER_GPT_URL` 设置后自动启用并带 `?ref=<token>`）。
  5. 「Browse products」→ `/products`。
  6. 语言切换 EN / 中文（沿用 `MeasureWizardClient.tsx:34-35` 的 `tr()` 范式，状态存 `localStorage 'ad_lang'`，与其它页一致的键名——Opus 先 grep 现有键名，有就复用）。
- 服务端副作用：**cookie 由 `middleware.ts` 种**（2026-08-22 补修 B1 定案）。matcher 加 `'/r/:token'`，命中且 token 过 `TOKEN_RE` 就在 `NextResponse.next()` 上 `set-cookie: ad_ref=<token>`（httpOnly, lax, secure in prod, 90 天, path `/`），然后立刻 return —— 必须排在 `getJwtSecret()` 的拒绝分支**之前**，否则没配 `JWT_SECRET` 的环境会把落地页一起 500 掉。
  - 为什么不是 `page.tsx` / route handler：Server Component 在 Next 15 里不能写 cookie；而先前那版把 cookie 交给客户端 POST `/api/referral/claim`，那个路由**按 IP 限流 30 次 / 10 分钟** —— 微信内置浏览器、公司 NAT、展会 Wi-Fi 都是共享出口 IP，一条链接在群里转开，前 30 个人之后的访客全部拿不到 cookie，归因静默失效。这不是边角情况，这就是推广链接的主场景。middleware 没有网络往返、没有限流、**禁用 JS 也照种**。
  - 无效 token 不落地（正则挡掉）；就算落地，AAPP 的 `resolveReferralCore` 也只会返回 null 当成没有推荐人 —— 零副作用，所以 middleware 里不做网络校验。
  - `/api/referral/claim` **保留但降级**：只负责 `logLeadEvent({anonId, type:'referral_visit', …})` 与 `recordReferralVisit(token,'/r',ua)`，cookie 顺手再种一次（幂等）。限流口径改为 **每 token 每 IP 每 10 分钟 1 次**，与 AAPP 端 `referralVisit` 的 `_rateLimitAllow('refVisit', token+'|'+ip, 1, 600000)` 对齐（两边各记各的，口径不同就会对不上账）；被限流返回 200 + `deduped:true`，不是错误。
  - ⚠ middleware 里的 `REFERRAL_COOKIE` / `REFERRAL_TOKEN_RE` / `REFERRAL_COOKIE_MAX_AGE` 是从 `lib/referral.ts` **抄过去**的（edge runtime 不能 import 带动态 `import()` 的模块）。`referral.test.ts` 的「middleware 与 lib 的常量同步」组盯着它们别漂开。
- `Ask AI`：`window.dispatchEvent(new CustomEvent('ad:open-assistant', { detail: { ref: { token, type, displayName, discountPct }, prefill?: string } }))`。

### 1.3 `StoreAssistant.tsx` 接收上下文

`:488-492` 的 `onOpen` 改为 `(e: Event) => { const d = (e as CustomEvent).detail; if (d?.ref) setRefCtx(d.ref); if (d?.prefill) setInput(d.prefill); openChat() }`。
打开时若 `refCtx` 存在且对话为空，插入一条**本地**助手开场白（不发请求）：customer → 「Hi! {displayName} sent you here — friends get {pct}% off. Tell me about your windows and I'll give you a quick estimate.」/ 中文版；partner → 「Hi! You were referred by {displayName}. …」。`refCtx` 也随 `/api/store/assistant` 请求体带 `ref: {token}`（服务端**不信任**它，只用 cookie）。

### 1.4 归因穿透（服务端，只信 cookie）

- `app/api/store/assistant/route.ts:444` 后加 `const refToken = getReferralFromRequest(request)`；`:448` 的 `logLeadEvent` meta 加 `ref: refToken || undefined`；`:560` 调用 `executeAssistantTool(block.name, block.input, userId, anonId, campaignId, customerTexts, refToken)`。
- `lib/assistantTools.ts`：
  - `ProjectOwnerCtx`（:250-254）加 `refToken?: string | null`；
  - `executeAssistantTool` 签名加第 7 参 `refToken: string | null = null`，`owner` 带上；
  - `buildAiSalesSummary`（:542）在 Campaign 行后加 `if (ctx.refToken) lines.push(`Referral: ${ctx.refToken}`)`；
  - `submit_website_inquiry`（:1613）`submitWebsiteInquiry({... , referral: owner.refToken ? { token: owner.refToken, page: 'assistant' } : undefined })`；成功后的 `logLeadEvent` meta 加 `ref`。
- `lib/aappIntake.ts`：`InquiryInput` 加 `referral?: { token: string; page?: string }`；body 加 `referral: input.referral && TOKEN_RE.test(input.referral.token) ? { token: input.referral.token, page: (input.referral.page||'').slice(0,120) } : undefined`；`InquiryResult` 加 `referral?: { applied:boolean; type?:string; label?:string }`，从响应透传。
- `app/api/consultation/route.ts:135-144`：`submitWebsiteInquiry({..., referral: refToken ? {token: refToken, page:'consultation_form'} : undefined})`，`refToken = getReferralFromRequest(request)`。
- `lib/leadScoring.ts`：`LEAD_SCORE_WEIGHTS` 加 `referral_visit: { points: 8, cap: 16 }`；`SIGNAL_LABEL` 加 `referral_visit: '推荐链接访问'`。

### 1.5 `/rewards/[token]/page.tsx`（新，customer 专属动态页）

- `generateMetadata`：`title:'Your Exclusive Rewards'`，`description:'Your Angel Drapery rewards: personal code, level, benefits and referral progress.'`，`openGraph.images:['/og-rewards.jpg']`，`robots noindex`，`canonical:/rewards/${token}`。**不在 meta 里放名字或码**（预览卡零 PII）。
- 数据：`fetchReferralPortal(token)`，`type!=='customer'` 或 null → `notFound()`；`export const dynamic='force-dynamic'`，响应头 `Cache-Control: no-store`。
- 版面（Client 岛 `RewardsClient.tsx`，全部双语，默认语言 = `portal.lang`）：
  1. 头部：「Hi {displayName}」+ 等级徽章（Member/Silver/Gold/Platinum/Diamond 五色，颜色沿用 `AAPP/app-loyalty.js:20-26` 的 color 值）。
  2. 专属码大字 + Copy。
  3. 当前权益：「{discountPct}% off every project · {freeVisitCredits} free service visit(s)」。
  4. 进度条：`qualifiedReferrals / nextTier.min`，文案「{needed} more referral(s) to {nextTier.label} ({nextTier.discountPct}% off)」；diamond → 「You're at the top level」。
  5. 五级阶梯表（`portal.tiers`）。
  6. **Share with friends**：`shareUrl`；按钮：Copy link / Web Share（`navigator.share` 可用时）/ SMS（`sms:?&body=`）/ WeChat 二维码（`qrcode` 依赖若仓库已有则用，否则用纯 SVG 实现的轻量 QR 库——Opus 先查 `package.json`）。文案「Your friends get {discountPct}% off, and you move up a level when they complete a project.」
  7. Google Review 邀请：「Enjoying your window treatments? A review helps a small family business.」→ 链接 `NEXT_PUBLIC_GOOGLE_REVIEW_URL`（未设置则隐藏整块）。**不得出现任何奖励/折扣字样**（合规）。
  8. **Notify me about my rewards** 开关：初值 `portal.smsOptIn`；`portal.smsOptedOut===true` 时开关禁用并显示「You replied STOP — text START to our number to re-enable」；切换 → `setReferralPortalPrefs` → 成功后 toast，失败回滚。小字「Reward updates only. Msg & data rates may apply. Reply STOP to opt out.」
  9. 页脚：公司 NAP（`layout.tsx:12-51` 的常量）。

### 1.6 `/partner/[token]/page.tsx`（新，合作方页）

- `type` ∉ agent/designer/contractor → `notFound()`。meta：`title:'Your Angel Drapery Partner Link'`，noindex。
- 版面：分享链接 + 二维码（大，可长按保存）+ 口述码 `AP-…`；统计「{referredLeads} referred · {signed} signed」；Share 按钮同 1.5-6；**W-9 区块**：`w9.uploaded===false` → 上传控件（pdf/jpg/png ≤8MB，前端 FileReader→base64 → `uploadPartnerW9`）+ 红字「Commission cannot be paid until your W-9 is on file / 佣金发放前须上传 W-9」；`uploaded && !verified` → 「W-9 received — pending review」；`verified` → ✅。**不显示任何金额**。

### 1.7 AI 估价 ×1.05（拍板 5.5-2）

新文件 `lib/customerEstimateFactor.ts`：

```ts
export const AI_ESTIMATE_FACTOR = 1.05
export const AI_SHOW_INSTALL_ESTIMATE = process.env.AI_SHOW_INSTALL_ESTIMATE === '1'   // 默认关（Eddie 2026-08-10 规则）
const SCALED_TOOLS = new Set(['get_hd_estimate','quote_shutter_estimate','quote_luma_estimate','quote_drapery_estimate','quote_roman_estimate','quote_hardware_estimate','quote_somfy_track_estimate','get_sundance_jc_estimate'])
const PRICE_KEYS = new Set([/* Opus 逐个 case 读返回对象，列出所有表示金额的键名，如 price, low, high, price_low, price_high, subtotal, per_panel, reference_low, reference_high … */])
export function applyCustomerEstimateFactor(toolName: string, result: unknown): unknown
```

- 只对 `SCALED_TOOLS` 生效；递归遍历对象/数组，键名 ∈ `PRICE_KEYS` 且值为有限数 → `Math.round(v * 1.05)`；其它原样。**`quote_store_product` 永不乘**（网店真实结账价）。
- 在 `executeAssistantTool` 末尾统一套用：把 `switch` 包成内部函数 `run()`，`return applyCustomerEstimateFactor(name, await run())`。**不改各 case 内部**。
- `logLeadEvent` 的 `value` 仍记**真价**（在 case 内已记，套用在其后，不受影响）——断言。
- `REFERENCE_PRICE_DISCLOSURE`（:38）追加一句：`'The final quote after the in-home measure is usually a little lower than this reference.'`（把惊喜预告出来）。
- `AI_SHOW_INSTALL_ESTIMATE===true` 时：`quote_shutter_estimate`（:1252 被删的 `r.installAmount`）等 case 里把 `install_estimate: r.installAmount`（**真值，不乘**）加回返回对象，并把 disclosure 替换为允许报安装约数的版本；默认 false 时行为与今天完全一致。Opus 只需在有 `installAmount` 的 case 加条件字段，不重写 prompt。

### 1.8 Campaign 卡片 token（企划 §1.5 入口 ④，最小版）

`lib/campaigns.ts`：`campaigns` 表 `ALTER TABLE ... ADD COLUMN IF NOT EXISTS referral_token varchar(64)`；`createCampaign` 成功后调 AAPP `ensureCampaignReferralLink`（onCall 需登录——改为 P0 新增的 HTTP 版 `referralLookup` 不够；**本期先这样**：admin 在 AAPP 侧建 campaign link 拿到 token，在网站 admin campaigns 页新增「Referral token」输入框手填保存；`/c/<slug>` 在 `campaign.referral_token` 存在时**同时**种 `ad_ref` cookie）。漏斗报表 `listCampaignsWithFunnel` 加 `referral_visits` 列（`type='referral_visit' AND meta->>'token' = c.referral_token`）。全自动建链留 P2b。

### 1.9 静态资源

`public/og-referral.jpg`、`public/og-rewards.jpg`（1200×630）：Opus 用现有 `og-image.jpg` 的版式改文案（「Free curtain AI consultant」/「Your Exclusive Rewards」），极简留白（Eddie 的设计口味：memory `postcard-mailer` ——「极简、留白、有钩子」，不要照片+卖点列表）。先出 PNG 交 Eddie 目检，再替换。

---

## 2. 测试（必须）

`packages/shared` 或 `apps/web/src/lib/*.test.ts`（照 `aappIntake.test.ts` 风格，vitest）：

1. `getReferralFromRequest`：合法 cookie 取到；非法字符 → null；与 `ad_campaign` 并存互不影响。
2. `applyCustomerEstimateFactor`：每个 SCALED_TOOLS 工具各一个真实形状的 fixture（从各 case 返回体复制），所有金额键 ×1.05 四舍五入；非金额键（`area_sqft`、`quantity`、`billed_size`）不变；`quote_store_product` 原样；`error` 对象原样；嵌套数组 `lines[].price` 也乘。
3. `submitWebsiteInquiry` 带 `referral` 时 body 含 `referral.token`；token 不合法时不带；响应 `referral` 字段透传。
4. `buildAiSalesSummary` 含 `Referral: <token>` 行（ctx.refToken 存在时）。
5. `LEAD_SCORE_WEIGHTS.referral_visit` 存在且 `computeLeadScore` 两次 visit = 16 封顶。
6. `tsc --noEmit` exit 0；`vitest` 全绿。

端到端（沙箱 `AAPP_REFERRAL_MOCK=1 npm run dev`）：
7. `/r/<token>` 200，`<meta property="og:title">` 含 displayName，**响应头直接**含 `set-cookie: ad_ref=…; HttpOnly; Max-Age=7776000`（由 middleware 种，不依赖任何客户端 JS —— `curl -sI` 就能看到）；`/r/bad` → 302 `/`。
   补充验收：禁用 JS 的浏览器上下文打开 `/r/<token>` 后再提交 `/api/consultation`，AAPP 端收到的 body 必须含 `referral.token`。
8. 打开 AI → 开场白含推荐人名 → 输入 555-0100 测试号走 `submit_website_inquiry` → `aappIntake` 被 `test_identity_blocked` 拦下（预期），但日志里可见 body 含 `referral.token`（加 `console.debug` 仅在 `NODE_ENV!=='production'`）。
9. `/rewards/mock-customer-…` 渲染五级表、进度条、开关；`/rewards/mock-agent-…` → 404；`/partner/mock-agent-…` 渲染 W-9 上传块。
10. `/rewards/...` 响应头 `cache-control: no-store`；`/r/...` 的 `robots` meta 为 noindex。

---

## 3. 交付

- 小步提交（每个小节一个 commit）；commit 信息前缀 `feat(referral):`。
- 回执 `OPUS-回执-推广系统P1-<日期>.md`：commit 列表、tsc/vitest 输出、两张 OG 图 PNG、需要 Eddie 配置的 env（`AAPP_WEBINTAKE_SECRET` 已有；新增 `AAPP_REFERRAL_BASE`(可选)、`NEXT_PUBLIC_GOOGLE_REVIEW_URL`、`NEXT_PUBLIC_CUSTOMER_GPT_URL`(留空)、`AI_SHOW_INSTALL_ESTIMATE`(留空)）。
- 部署由 Eddie `git push origin main`。

## 4. 禁改清单

定价目录与两个核价入口；`contactClaimGuard` 三道闸与 555 号段拦截；`resolveProjectOwner` / `mergeAnonProjectIntoUser`；`/c/[slug]` 现有行为（只加 `ad_ref` 并存）；`knowledge.generated.ts`（本期不改知识库）；`middleware.ts`（`/r` `/rewards` `/partner` 本就不在 matcher 内，不要加）。
