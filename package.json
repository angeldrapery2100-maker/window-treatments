{
  "name": "window-treatments",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "clean": "turbo clean",
    "check:colors": "node scripts/check-color-codes.js",
    "check:assistant": "node --experimental-strip-types scripts/check-assistant-failure-codes.js",
    "check:partner": "node scripts/check-partner-pages.js"
  },
  "devDependencies": {
    "@types/pg": "^8.16.0",
    "turbo": "^2",
    "typescript": "^5.7"
  },
  "packageManager": "npm@10.9.2",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pg": "^8.18.0"
  }
}
