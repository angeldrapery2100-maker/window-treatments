# 需要你做的事 —— 一步一步照着做就行

面料库和 /design 页的代码我已经写完并提交了(4 个 commit,还没推)。剩下五件事必须你来,按顺序做,**从第 1 件开始**。

每一步我都写了「怎么做 / 看到什么算成功 / 出问题怎么办」。看不懂的直接把屏幕上的字发给我。

> **打开终端的方法**:按 `⌘ + 空格`,输入 `终端` 或 `Terminal`,回车。
> 下面所有以 `$` 开头的行,是你要**复制粘贴进终端再按回车**的命令(`$` 本身不要复制)。

---

## 第 1 件 · 重新登录 Claude 桌面 App(2 分钟)

**为什么**:我们这次会话跑到一半,你这台电脑在 Claude 里的登录态过期了。现在我还能读写你的文件,但**没法把图片传回聊天窗口给我自己看**——所以面料打标的人工抽检我做不完(第 2 件事就卡在这)。

**怎么做**
1. 打开 Claude 桌面 App。
2. 顶部应该有一条提示登录过期的横幅,点它,重新登录一次。
3. 回来跟我说一句「登录好了」。

**成功的样子**:横幅消失。

---

## 第 2 件 · 看两张对照图,把错的编号告诉我(10 分钟)

**为什么**:一万款布的颜色和花纹是我用规则 + 色卡照片自动打的标。任务书的验收线是「抽 100 款人工看,颜色错 ≤10%,花纹错 ≤15%」。**这一步只有人眼能做。**

**怎么做**

1. 打开访达(Finder),按 `⌘ + ⇧ + G`,粘贴这个路径后回车:

   ```
   /Volumes/SSD2T/Projects/outputs/fabric_webp/qa
   ```

2. 双击 `FINAL-taxonomy-check-page1.png`,看完再看 `FINAL-taxonomy-check-page2.png`。

3. 每一格底下有三行小字:

   ```
   17 Grey +Blue        ← 编号 · 我判的主色族 · (+后面是备用可搜色族)
   Print                ← 我判的花纹
   "Mojito"             ← 厂商起的色名(仅供参考,它经常瞎起)
   ```

4. **你只需要挑毛病**:哪一格的**主色族**明显不对(比如图明明是绿的却写 Yellow),或者**花纹**明显不对(比如明明是条纹却写 Solid),把编号记下来。

5. 把编号发给我就行,格式随意,例如:

   > 颜色错的:11、17、38、44
   > 花纹错的:30、59

**判断尺度(别太苛刻)**:
- 米色 / 奶油色 / 灰褐色 这几个中性色互相认错,**不算错**——人也分不清。
- 图案是彩色的但底色是米色,我标了米色,**不算错**(底色为准是行业惯例)。
- 明明是绿的写成黄的、明明是黑的写成棕的 —— **算错**。
- `Print` 表示「有重复图案但我叫不出名字」,不是错。

**我收到编号之后做什么**:全部写进 `fabric-taxonomy.overrides.json`(人工覆盖表),以后重跑数据脚本也不会被冲掉。如果错得成片,我会去调规则而不是一个个改。

---

## 第 3 件 · 把面料图片传上 R2(30–60 分钟,大部分时间在等)

**为什么**:10,845 张色卡图已经在你电脑上转成网页格式了(两个尺寸,共 21,688 个文件、1.86 GB),但**必须传到 R2(你的图片服务器)网站才看得见**。我这边的沙箱没有网络,传不了,只能你来。

**在你传完之前**:网站的面料库能正常筛选、能点开、能估价,只是每张卡片显示的是**那块布的主色块**而不是照片。不影响别的功能。

### 3.1 先拿到 R2 的钥匙(只需要做一次)

> **2026-08-11 更新**:你刚才那条 `npx vercel env pull` 是在**仓库根目录**跑的、而且**没带参数**,所以 Vercel 拉的是 **development** 环境,存成了根目录的 `.env.local`。我检查过了,那份里 `R2_ACCESS_KEY_ID` 和 `R2_SECRET_ACCESS_KEY` **是空的** —— 所以这一步还没成。下面重来一次,注意两点:**要先 `cd` 进 `apps/web`**,**要带上后面那两个参数**。

```
$ cd /Volumes/SSD2T/Projects/window-treatments/apps/web
$ npx vercel env pull .env.production.local --environment=production
```

拉完检查 —— **不能只看有没有值,要看值长不长得像钥匙**(真的 Access Key ID 是 32 位十六进制):

```
$ grep -E '^R2_ACCESS_KEY_ID=' .env.production.local
```

- 显示一长串 32 位字母数字 → 成功,跳到 3.2。
- 显示 **`[SENSITIVE]`** → Vercel 把这个变量标成了 Sensitive,**拉不出来**(2026-08-11 实测就是这个结果)。走下面的「手动拿钥匙」。
- 什么都没显示 → 也走「手动拿钥匙」。

> 上传脚本现在会自己识别 `[SENSITIVE]` 这种占位符并直接报错,不会拿着假钥匙去撞两万次墙。

顺手把刚才拉错的那个文件删掉,免得以后看着乱:

```
$ rm /Volumes/SSD2T/Projects/window-treatments/.env.local
```

<details>
<summary><b>手动拿钥匙(5 分钟,现在就要做这个)</b></summary>

1. 浏览器打开 https://dash.cloudflare.com/ ,登录。
2. 左边菜单点 **R2 Object Storage**。
3. 页面右上角点 **Manage API tokens**(有的版本叫 Manage R2 API Tokens)→ **Create API token**。
4. 这样填:
   - **Token name**:`fabric-upload`
   - **Permissions**:选 **Object Read & Write**
   - **Specify bucket(s)**:选 **angel2100**
   - TTL / 到期时间:不用改
5. 点 **Create API Token**。
6. 下一页会给你 **Access Key ID** 和 **Secret Access Key** —— **这两串只显示这一次**,先复制到备忘录里。
   (页面上还会给一个 S3 endpoint,不用管,脚本自己会拼。)
7. 用「文本编辑」打开这个文件:

   ```
   /Volumes/SSD2T/Projects/window-treatments/apps/web/.env.production.local
   ```

   找到这两行,把值填进等号后面的双引号里(**引号要留着**):

   ```
   R2_ACCESS_KEY_ID="粘贴 Access Key ID"
   R2_SECRET_ACCESS_KEY="粘贴 Secret Access Key"
   ```

8. 保存,关掉。

</details>

### 3.2 先小传 180 个文件试水

```
$ cd /Volumes/SSD2T/Projects/window-treatments
$ set -a && . apps/web/.env.production.local && set +a
$ node apps/web/scripts/upload-fabric-images.mjs --only=Kaslen_Linen
```

**成功的样子**:最后一行是

```
Done: 180 uploaded, 0 already present, 0 failed.
```

然后把这个网址贴进浏览器,**应该能看到一小块米色亚麻布**:

```
https://pub-9090ea94bda94d6daf755d6ce4b62812.r2.dev/fabric-swatches/thumb/Kaslen_Linen/1440_Brussels_Bamboo.webp
```

看得到图 → 钥匙对了,继续 3.3。
看到 404 或一串报错 → **停下来把终端里的字发给我**,别往下走。

### 3.3 传全部

```
$ node apps/web/scripts/upload-fabric-images.mjs
```

- 会先打印 `21508 objects to upload · 1.85 GB`。
- 每传 500 个打印一行进度,带剩余时间估算,例如 `2000/21508 · 38/s · ~8 min left`。
- **这期间别关终端窗口**,电脑可以照常用。
- 传完最后一行是 `Done: 21508 uploaded, 0 already present, 0 failed.`

**中途断了怎么办**:直接把同一条命令再跑一遍。脚本记着传过哪些,只补没传的,不会重复花时间。

**最后有几个 failed**:再跑一遍同一条命令就行,它会自动重试失败的。

---

## 第 4 件 · 推代码 + 上线后自己点一遍(20 分钟)

### 4.1 推

```
$ cd /Volumes/SSD2T/Projects/window-treatments
$ git log --oneline -4
```

应该看到这四条(最上面是最新):

```
290fe24 HD page fabric preview + the three doors into /design
5d901ab design: /design shell, hardware matrix and reference estimate (task D)
90a7398 fabric library: /fabrics grid, detail pages and API (task C)
7d2ab77 fabric library: data pipeline + taxonomy (task A/B)
```

确认无误后:

```
$ git push origin main
```

Vercel 会自动开始构建,大概 3–5 分钟。

> ⚠️ **注意**:沙箱里跑不了 `next build`,我只能用 `tsc`(类型检查)和 `vitest`(单元测试)把关,两项都全绿。**真正的构建结果以 Vercel 为准**——如果 Vercel 构建红了,把报错整段发我。

### 4.2 上线后照着点(每条都试一下)

| # | 打开这个 | 应该看到 |
|---|---|---|
| 1 | `/fabrics` | 一万多款布的网格,左边一排筛选;往下滚会不断加载更多 |
| 2 | 左边勾几个颜色 + 花纹 | 数量变少;**地址栏跟着变**;复制地址发到微信给自己,再点开 → **同一屏** |
| 3 | 手机上打开 `/fabrics` | 顶部有「Filters」按钮,点开是侧边抽屉;滚动不卡 |
| 4 | 点任意一张布的**心形** | 变橙色;左边勾上 `My Fabrics (1)` 只剩它 |
| 5 | 点开一张布 → `Design with this fabric` | 跳到 `/design`,左侧面料条里选中的就是它 |
| 6 | `/design` 填宽 96、高 84 → 点 `See a reference estimate` | 出现「窗帘 / Hardware / 合计」三行数字 + 免责小字 |
| 7 | `/design` 款式选 **Grommet** | Hardware 里 aluminum track 和 H-rail **变灰点不动**,只剩 wood pole |
| 8 | 直接开 `/design?heading=grommet&hw=alu_track` | 自动纠正成 wood pole(而不是报错) |
| 9 | `/design` 宽填 400 | 出现「超出范围,请联系顾问」的提示,不是红色报错 |
| 10 | `/design` 底部 `Request a consultation` | 跳 /contact,**留言框里已经填好了整套设计参数** |
| 11 | `/fabrics/alendel::arch::blueberry` | 显示「Price on consultation」(这是一款 AAPP 里还没价的布) |
| 12 | `/products/handcrafted-drapery` | 首屏主按钮变成 **Design Your Drapery**;往下滚有一条「Explore our fabric library」横向色卡 |
| 13 | 顶部导航 | 多了一个 **Design** |

**第 6 条出不了数字**是最可能出问题的地方(它要连 AAPP 报价接口)。如果显示「由顾问报价」但你选的是普通褶帘 + 有价的布,把 Vercel 的 Functions 日志发我。

---

## 第 5 件 · 四个需要你拍板的(想清楚回我一句话就行)

### ① Grommet(打孔帘)现在报不出价 —— 要不要补?

**现状**:AAPP 的窗帘报价引擎里**根本没有 grommet 这个款式**,只有 2 褶/3 褶(pinch、tailored)和六档蛇形帘。所以我在网站上让客户能选 grommet(3D 那条线需要它),但估价面板直接显示「由顾问报价」。

**我为什么没糊弄**:最省事的做法是拿 2 褶的价当 grommet 的价,但那会让网站报价和你软件里的报价对不上——这是死线,我没碰。

**你要定的**:
- (A) 就这样,grommet 走人工报价 —— 不用做任何事。
- (B) 要它能自动报价 —— 那得在 AAPP 里加一个 grommet 款式(用料系数、加工费),加好告诉我 styleKey 叫什么,我这边一行配置就通了。

### ② 蛇形帘(Wave)我固定按 100% 满度报价,对吗?

AAPP 里蛇形帘有六档:`cn_6cm`、`cn_7cm`、`us_60`、`us_80`、`us_100`、`us_120`。客户在网站上只看到一个「Wave / ripplefold」,**我按 `us_100` 报,并在估价结果里写明「Assumed: priced at 100% ripplefold fullness」**。

**你要定的**:
- (A) 就用 us_100,不给客户选 —— 不用做任何事。
- (B) 换一档默认 —— 告诉我哪一档。
- (C) 让客户自己选 —— 我加一排选项。

### ③ 每码价要不要显示?

现在是**关着的**(`NEXT_PUBLIC_SHOW_FABRIC_PRICES` 默认 off)。关着的时候:
- 布卡片上**不显示** `$52/yd` 这种字样;
- **估价功能照常工作**;
- `$ / $$ / $$$ / $$$$` 这个价格区间筛选**照常能用**。

**要打开**:在 Vercel 项目 → Settings → Environment Variables 里加一条 `NEXT_PUBLIC_SHOW_FABRIC_PRICES` = `true`,重新部署一次即可。

**我的建议**:先关着上线看一周。显示单价会让客户拿着 $/yd 去比价,而你的价值在做工,不在布价。

### ④ /design 的 8 款默认布,认不认?

客户第一次进 /design、还没收藏任何布的时候,会看到这 8 款(我按「有价 + 有图 + 素色或织纹 + 中档价位 + 跨色族跨品牌不重样」自动挑的):

```
carole::beginners-luck::flint      alendel::soria::grey
alendel::tweed::cliff              carole::daily-basis::cloud
alendel::soho::sailor              alendel::switch::leaf
carole::cartwright::sand           alendel::mystery::sparrow
```

上线后在 `/design` 上直接就能看到它们长什么样。想换的话,改这个文件里的 id 就行,**重跑数据脚本不会覆盖你的修改**:

```
/Volumes/SSD2T/Projects/window-treatments/apps/web/src/data/fabric-featured.json
```

id 的写法是 `品牌::花型::颜色`,在 `/fabrics` 上点开任意一款布,地址栏里就是它的 id。

---

## 顺手清理(可选)

我在仓库里留了几个 `_to_delete/` 文件夹(挂载盘上我删不掉文件,只能挪进去)。你可以直接扔掉:

```
$ cd /Volumes/SSD2T/Projects/window-treatments
$ rm -rf _to_delete _to_delete_tmp3 _to_delete_tmp4 _to_delete_tmp5 _to_delete_tmp6
```

---

## 一句话优先级

**第 1 件(重新登录)→ 第 3 件(传图)→ 第 4 件(推代码+验收)** 是让网站真正好看、能用的最短路径。
**第 2 件(看图挑错)** 决定筛选准不准,可以在传图的时候顺手做。
**第 5 件(拍板)** 不着急,想清楚了随时告诉我。
