# OPUS 回执 — 推广系统 P1（网站落地页 / Rewards / Partner + 归因直通 + AI 估价 ×1.05）

日期：2026-08-22　执行：Opus（Claude Cowork 会话）　仓库：`window-treatments`（`apps/web`）
对应任务书：`OPUS-任务书-推广系统P1-网站-2026-08-22.md`

任务书 §1.1–§1.9 全部实现，§2 单测全部写完并通过。**代码已提交到本地 main，未 push**（部署仍是 Eddie 的动作）。

---

## 1. Commit 列表（5 个，均以 `feat(referral):` / `test(referral):` 开头）

| commit | 内容 |
| --- | --- |
| `fd56d76` | `lib/referral.ts` + `referral.mock.ts` + `lib/uiLanguage.ts` + `/r/<token>` 落地页 + `/api/referral/claim` |
| `1b11b58` | 归因穿透：assistant route / assistantTools / aappIntake / consultation / leadScoring / StoreAssistant |
| `b908ce7` | `/rewards/<token>`、`/partner/<token>`、`/api/referral/prefs`、`/api/referral/w9`、服务端二维码、no-store+noindex 响应头 |
| `8a1f0ab` | `lib/customerEstimateFactor.ts`（×1.05）+ 价格披露话术 + `AI_SHOW_INSTALL_ESTIMATE` + campaign `referral_token` |
| `7c45686` | 5 个测试文件（31 条新断言）+ 两张 OG 图 |

## 2. 验证（军规两项）

```
cd apps/web && ../../node_modules/.bin/tsc --noEmit     → exit 0
cd apps/web && npx vitest run                           → 24 files / 248 tests 全绿（新增 31 条）
cd packages/shared && npx vitest run                    → 6 files / 102 tests 全绿（定价引擎零改动）
```

新增测试文件：`referral.test.ts`、`customerEstimateFactor.test.ts`、`leadScoringReferral.test.ts`、
`aiSalesSummaryReferral.test.ts`，以及 `aappIntake.test.ts` 的 referral 用例。

**沙箱跑不了 `next build` / `next dev`**（linux/arm64 的 SWC 二进制在挂载盘上加载失败，与历史结论一致），
所以任务书 §2 的端到端第 7–10 条（真实 HTTP 状态码、`set-cookie`、`og:title`、响应头）**没有在本地跑起来**，
需要在 Vercel preview 上确认。可验证的等价物已经用单测覆盖（cookie 解析、token 校验、×1.05 形状、归因 body）。

## 3. 与任务书的四处偏差（都做了，理由如下）

1. **`/r/<token>` 的 cookie 不在 Server Component 里种，也没有走 302 到 `/view`。**
   任务书给的两个选项里，302 那条会让分享出去的 URL 不再是承载 OG 卡片的那一个——微信 / iMessage / Facebook
   抓的是被粘贴的那条链接，跳转后的预览卡不保证带上推荐人名字，而"{推荐人} invited you"正是这张卡的全部价值。
   所以页面留在 `/r/<token>`（带完整 metadata），cookie 由客户端岛在 mount 时 POST `/api/referral/claim` 种下：
   服务端重新校验 token（backend 不认就不种）、种 `ad_ref` + `ad_anon`、记 `referral_visit`、回传 `referralVisit`。
   首屏之后、任何转化动作之前 cookie 就已就位；"Ask AI"按钮在 claim 还在飞的时候会等 350ms 再开聊天。
   代价：关掉 JS 的浏览器不会被归因（该页的主 CTA 本来就是需要 JS 的 AI 聊天）。

2. **×1.05 同时缩放"显示字符串"，不只是数字键。**
   各报价工具返回的是 `price: 1623` **和** `reference_price: "$1,623"` 两份，模型被要求照抄后者。
   只乘数字键的话，模型念出来的仍是原价，整个因子等于白做（而且 priceClaimGuard 的 2% 容差也拦不住，
   因为原价字符串就在 tool 结果里）。所以 `reference_price` / `reference_range` / `say_it_as` / `price_display`
   里的每个 `$金额` 也一并按同一因子重写。已在单测里逐个工具锁死。

3. **二维码用了新依赖 `qrcode`（+ `@types/qrcode`），服务端渲染成 PNG data URL。**
   仓库里原本没有 QR 依赖；admin 活动页用的是第三方 `api.qrserver.com`。合作方/客户页面把 token 发给第三方
   不合适，所以装了 `qrcode`（1.5.4，纯 JS，无原生依赖）在服务端出图。用 PNG 而不是内联 SVG，是因为手机
   "长按保存"只对真正的位图生效——而合作方页面的二维码就是要被存下来、印到名片和传单上的。

4. **五级颜色是我按 AAPP 配色写的占位值。** `AAPP/app-loyalty.js` 这次会话读不到（AAPP 目录没挂载），
   `referral.mock.ts` 里的 `MOCK_TIERS.color` 与 `RewardsClient.TIER_FALLBACK_COLOR` 是占位；
   真实颜色由 P0 的 `referralPortal` 在 `tiers[].color` 里下发即可自动覆盖，无需改网站代码。

## 4. 需要 Eddie 配置的环境变量（Vercel）

| 变量 | 必需 | 说明 |
| --- | --- | --- |
| `AAPP_WEBINTAKE_SECRET` | 已有 | 复用；所有 referral 请求都会带 `x-ad-key` |
| `AAPP_REFERRAL_BASE` | 可选 | 默认 `https://us-central1-angel-drapery.cloudfunctions.net` |
| `AAPP_REFERRAL_MOCK` | **生产禁止设置** | 本地调试用：`=1` 时走 `referral.mock.ts` 假数据 |
| `NEXT_PUBLIC_GOOGLE_REVIEW_URL` | 建议填 | 不填则 /rewards 的 Google 评价整块隐藏 |
| `NEXT_PUBLIC_CUSTOMER_GPT_URL` | 先留空 | 留空时 /r 的「Open in ChatGPT」显示为 disabled + Coming soon；填了会自动带 `?ref=<token>` |
| `AI_SHOW_INSTALL_ESTIMATE` | 先留空 | `=1` 才允许 AI 报安装约数（默认关，行为与今天完全一致）|

## 5. P0（AAPP 侧）需要对齐的接口形状

网站按下面的形状解析；字段缺失都能降级，但对齐了才不用改网站。

- `GET /referralLookup?t=<token>` → `{ ok, referrerType|type, displayName, discountPct, tierLabel?, tierLabelCn?, referralCode?, active }`
- `POST /referralVisit` ← `{ token, page, ua }`
- `GET /referralPortal?t=<token>` → `{ ok, type, displayName, lang, referralCode, shareUrl, discountPct, freeVisitCredits,
  qualifiedReferrals, tierKey, tierLabel, tierLabelCn, nextTier{key,label,labelCn,min,discountPct},
  tiers[同上+color], smsOptIn, smsOptedOut, stats{referredLeads,signed}, w9{uploaded,verified}, active }`
- `POST /referralPortalPrefs` ← `{ token, smsOptIn }` → `{ ok, smsOptIn }`
- `POST /partnerW9Upload` ← `{ token, file(base64 无前缀), mime }` → `{ ok }`
- `websiteInquiry` 增加可选入参 `referral: { token, page }`，返回可选 `referral: { applied, type, label }`。

`/rewards` 只认 `type==='customer'`，`/partner` 只认 `agent|designer|contractor`，其它一律 404。

## 6. 沙箱留下的一件事（**Eddie 上手前先做**）

在 Mac 上先跑一次：

```bash
cd /Volumes/SSD2T/Projects/window-treatments && npm install
```

原因：这次会话在 Linux 沙箱里执行 `npm install qrcode`，npm 顺手把 node_modules 里 **macOS 原生二进制**
（`@next/swc-darwin-arm64`、`@esbuild/darwin-arm64`、`@rollup/rollup-darwin-arm64`、`@img/sharp-darwin-arm64`、
`turbo-darwin-arm64`）的目录清空了——因为 node_modules 是通过挂载盘共享的同一份。`npm install` 会把它们装回来。
`package.json` / `package-lock.json` 本身是干净的（只新增了 qrcode 两条）。

## 7. 本期没做 / 留待后续

- 全自动建 campaign 推荐链（任务书 §1.8 明确留 P2b）：现在是 admin 在 AAPP 建好后，把 token 粘到
  `/admin/campaigns` 每行的「Referral token」输入框里（失焦即存），`/c/<slug>` 随后会同时种 `ad_ref`。
- 客户版 GPT（P3）：按钮已就位，disabled + Coming soon。
- OG 图文案沿用现有 `og-image.jpg` 版式改的（极简留白，无照片无卖点列表），如果要换措辞随时说。
