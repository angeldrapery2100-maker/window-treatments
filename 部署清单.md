# Angel Drapery 媒体优化 + 部署清单

> 复用流程:把图片/视频优化后上线。基于 2026-06 这次实操整理,含踩过的坑。
> 关键背景:**生产媒体走 R2 CDN**(`NEXT_PUBLIC_CDN_URL` 已配),`public/videos`、大图都被 gitignore、**不进 git**。所以媒体要上传到 R2,不是提交到仓库。

---

## 0. 前置条件
- `ffmpeg`(视频转码用):`brew install ffmpeg`,`ffmpeg -version` 能输出版本
- `sharp`(图片转码用,已是 web 依赖)。若报 `Could not load the "sharp" module ...darwin-arm64`:
  ```
  cd ~/Projects/window-treatments/apps/web
  npm install --include=optional sharp     # 或 npm install --os=darwin --cpu=arm64 sharp
  ```
- `wrangler` 已登录 Cloudflare(R2 上传用)。bucket 名:`angel2100`

## 1. 转码媒体(脚本,不动原文件 → 输出到 apps/web/public-optimized/)
```
cd ~/Projects/window-treatments
node scripts/optimize-media.mjs --dry-run   # 预览
node scripts/optimize-media.mjs --images    # 图片 → AVIF/WebP(较快,先跑)
node scripts/optimize-media.mjs --videos    # 视频 → 1080p MP4 + WebM + poster(很慢)
```
- 视频很慢(VP9 + 大文件),放着跑完。
- `public-optimized/` 已加入 `.gitignore`,**永不提交**(约 1.9GB)。

## 2. 更新代码引用(仅视频:.mov → .mp4)
脚本把 `.mov` 转成 `.mp4`,需同步代码里的引用:
```
cd ~/Projects/window-treatments/apps/web/src
sed -i '' 's/\.mov/\.mp4/g' components/home/ProjectsSection.tsx lib/gallery-videos-data.ts
```
注意:**不要**改 `ImageCropper.tsx`(那里的 `.mov` 是上传格式白名单,不是文件路径)。
图片不需要改引用——`next/image` 会在线把原图转 AVIF/WebP。

## 3. 上传优化后的视频到 R2(图片可不传,next/image 已覆盖)
```
cd ~/Projects/window-treatments/apps/web
for f in public-optimized/videos/projects/*.mp4; do
  npx wrangler r2 object put "angel2100/videos/projects/$(basename "$f")" \
    --file="$f" --content-type="video/mp4" --remote
done
```
⚠️ **坑:上传可能部分失败/中断,务必全部核查**(见第 6 步)。漏传的会 404 → 悬停不播。

## 4. 本地验证
```
cd ~/Projects/window-treatments
npm install        # 同步依赖(如 vitest)
npm run build      # 必须通过
npm run dev        # localhost:3000 逐页看
npm test -w @window-treatments/shared   # 定价单测 9/9
```
逐页重点看(next/image 迁移过的):首页、各 collection 落地页、产品详情、store 购物车/结算——图片有没有错位/拉伸/塌成 0 高。
注:本地若没配 `DATABASE_URL`,CMS/DB 版块会显示占位,属正常,生产有 DB。

## 5. 部署(git push → Vercel 自动构建)
```
cd ~/Projects/window-treatments
git add -A         # public-optimized 已被忽略,不会进来
git commit -m "..."
git push origin main
```
- 只想上部分改动时,用 `git add <具体文件>` 精确提交,别一次性 `-A`。

## 6. 线上核查(关键!确认视频都到位)
部署 Ready 后,在 angel-drapery.com 控制台跑(或让我用 Chrome 跑):
```js
(async () => {
  const names = ['landscape-01','landscape-02','landscape-03','landscape-04','landscape-05','landscape-06','landscape-07',
    'portrait-01','portrait-02','portrait-03','portrait-04','portrait-05','portrait-06','portrait-07','portrait-08','portrait-09'];
  const missing = [];
  for (const n of names) {
    const r = await fetch(`/videos/projects/${n}.mp4?v=${Date.now()}`, { method: 'HEAD' });
    if (r.status !== 200) missing.push(`${n} (${r.status})`);
  }
  return missing.length ? { missing } : 'all 16 OK';
})();
```
漏传的补传(把名字填进去):
```
for n in landscape-01 landscape-02; do
  npx wrangler r2 object put "angel2100/videos/projects/$n.mp4" \
    --file="public-optimized/videos/projects/$n.mp4" --content-type="video/mp4" --remote
done
```

## 7. 环境变量(Vercel)
- 改 env 后需 **Redeploy** 才生效(Vercel 会提示)。
- 密钥类(JWT_SECRET 等)由你本人在 Vercel 输入,不要明文存本地仓库。
- `NOTIFICATION_EMAIL` = 表单通知收件地址(当前 `admin@angel-drapery.com`),与页面上展示的联系邮箱(CMS 文案)是两回事。

---

## 收尾可选项
- 清理 R2 上的旧 `.mov`(已被 `.mp4` 取代,省空间):`npx wrangler r2 object delete "angel2100/videos/projects/<name>.mov" --remote`
- 把页面展示的联系邮箱也改成 admin@:后台 CMS 改。
