# OPUS 任务书 — 网站第一阶段:结构收敛 + 畅销款上线(2026-08-11)

> 仓库:`/Volumes/SSD2T/Projects/window-treatments`(Next.js monorepo,改动集中在 `apps/web`)。
> 背景:Eddie 2026-08-11 拍板网站总方向(见 `docs/网站-AAPP对接总体蓝图-2026-08-11.md`)。本阶段只做:**双轨定位文案 + Store 收敛为精选畅销款 + 估价免责措辞统一 + 两处小补**。面料库、3D 嵌入、AI、AAPP 端改动都不在本阶段。
> 交付节奏:按 T1→T5 顺序,每个任务独立 commit,小步提交。

## 军规(违反=返工)

1. 每个任务完成后跑:`cd apps/web && ../../node_modules/.bin/tsc --noEmit`(exit 0)+ `cd packages/shared && npx vitest run`(全绿)。
2. **禁改清单**:`packages/shared/src/pricing/**`(引擎与测试)、`lib/draperyFabricTiers.generated.ts`、`lib/productPricing.ts` 与 `api/store/pricing/calculate/route.ts` 的计价逻辑、`lib/aappIntake.ts`、`lib/hdPricing.ts` 的询问顺序与计价参数、`api/store/assistant/knowledge/**`(本阶段不动 AI 知识)。
3. 客户可见文案一律英文;不新增中文到 storefront。
4. `next build` 沙箱跑不了,以 tsc + vitest 为门槛(AGENTS.md)。
5. 不确定的现状先 recon 再动手;发现与本任务书描述不符,停下记录并按"回报格式"上报,不要自行扩大范围。

---

## T1 · 双轨定位文案(Store 侧)

**目标**:让访客一眼分清两条轨——Store = 标准款直接买、全美发货;Handcrafted Drapery = 全定制、走咨询。

**文件**:`apps/web/src/app/store/page.tsx`

在商品区(分类 tab / 商品网格)上方、页面标题区之下,插入定位条(样式融入现有页面,浅底细字):

```tsx
{/* Two-track positioning strip */}
<div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mb-8">
  <div className="rounded-lg bg-[#F7F6F3] px-5 py-4 flex flex-col md:flex-row md:items-center md:justify-between gap-3">
    <p className="text-sm text-gray-600">
      Ready-made &amp; standard-size window treatments — simple checkout, shipped nationwide.
    </p>
    <Link href="/products/handcrafted-drapery"
      className="text-sm font-medium text-[#12141C] underline underline-offset-4 hover:text-black whitespace-nowrap">
      Looking for fully custom drapery? Explore Handcrafted Drapery →
    </Link>
  </div>
</div>
```

**附带 recon 项(HD 页反向链)**:检查 `apps/web/src/app/products/handcrafted-drapery/page.tsx` 是否已有指向咨询/估价的明确 CTA(ConsultationWidget 或 /contact 链接)。若已有 → 不动,在回报里注明位置;若没有 → 在页面主内容区末尾加一条等价的反向条("Need something ready-made sooner? Shop standard sizes in our Online Store →" + 主 CTA "Request a design consultation" 链到 /contact)。

**验收**:/store 首屏能看到定位条;链接可达;移动端不换行溢出;lighthouse 无新增 CLS 元凶(有固定高度容器即可)。

---

## T2 · Store 收敛为精选畅销款(featured 驱动)

**目标**:Store 落地页默认只展示 Eddie 标记的 4–5 个畅销款;其余商品不出现在落地页(详情页直链仍可访问,不下架)。通过后台开关控制,不写死商品 ID。

**第一步 recon(必做,写进回报)**:
- 读 `apps/web/src/app/api/admin/products/[id]/featured/route.ts`,确认 products 表的 featured 字段名(is_featured / featured / …)与 admin 端开关位置。
- 读 `/api/store/products/route.ts` 返回结构,确认 featured 字段是否已随商品列表返回;没有则加进 SELECT(只加字段,不改过滤)。

**行为规格**(`apps/web/src/app/store/page.tsx`):
1. 商品加载后:`const featured = products.filter(p => p.<featured字段>)`。
2. **featured.length ≥ 1 时**:落地页只渲染 featured 商品,单一 "Best Sellers" 网格;隐藏 TYPE_TABS 分类 tab 与 Recommended 区(避免重复);每卡加角标:

```tsx
<span className="absolute top-3 left-3 z-10 bg-[#12141C] text-white text-[10px] font-semibold tracking-widest uppercase px-2.5 py-1 rounded-full">
  Best Seller
</span>
```

3. **featured.length === 0 时**:现有行为完全不变(安全回退,商店不会因没标记而空白)。
4. "from $xxx" 起价角标逻辑(starting-prices)照旧作用于 featured 卡。

**明确不做**:不改 `/api/store/products` 的过滤条件(其他 active 商品仍可被直链/购物车/结账使用);不动商品数据;不做"View all"入口(Eddie 定了只放畅销款,先收干净)。

**验收**:后台把 5 个商品标 featured 后,/store 只显示这 5 张卡且带角标、无分类 tab;取消全部 featured 后页面回到现状;任一 featured 商品可完整走完 加购→结账(Stripe 测试模式)。

---

## T3 · 估价免责措辞统一

**目标**:全站所有出价面共用一份免责文案,防止措辞漂移。

**新文件** `apps/web/src/lib/estimateCopy.ts`(完整内容,照抄):

```ts
// Single source of truth for estimate disclaimers (Eddie 2026-08-11):
// every surface that shows a reference price must render one of these
// verbatim — final quote / shipping / installation / fabric availability
// are confirmed by a sales consultant, never by the website.

/** Short form — under a price figure. */
export const ESTIMATE_DISCLAIMER_SHORT =
  '* Reference estimate only. Final pricing is confirmed by your sales consultant. ' +
  'Installation fees and applicable taxes are not included.'

/** Long form — fabric library / request-a-quote surfaces. */
export const ESTIMATE_DISCLAIMER_LONG =
  '* This is a reference estimate, not a quote. Final pricing, shipping, ' +
  'installation, and fabric availability are confirmed by your sales consultant.'
```

**替换点**:
1. `components/PriceEstimator.tsx` 结果卡里的硬编码免责段(现文案与 SHORT 一致)→ 改为 `{ESTIMATE_DISCLAIMER_SHORT}`。
2. grep `Reference estimate` / `estimate only` 全 `apps/web/src`,凡展示参考价的客户面(store 商品模板 StickyPriceBar 附近、whole-home 等)统一改用常量;后台 admin 页不用改。逐处列进回报。

**验收**:grep 后客户面无第二份手写免责文案;tsc 过。

---

## T4 · leadScoring 补 `store_estimate` 权重(小修)

**文件**:`apps/web/src/lib/leadScoring.ts`

现状:`SIGNAL_LABEL` 有 `store_estimate: '商店询价'`,但 `LEAD_SCORE_WEIGHTS` 缺该键 → 此类事件计 0 分。在 `LEAD_SCORE_WEIGHTS` 的 `sundance_jc_estimate` 行后加一行:

```ts
  store_estimate:        { points: 15, cap: 30 },
```

**验收**:`computeLeadScore([{type:'store_estimate'}])` 返回 15(在现有测试旁补一个用例即可);vitest 全绿。

---

## T5 · 全美发货与开门配置勾稽(只验证 + 报告,不改码不切换)

对照 `docs/go-live-checklist.md` 逐项核实并输出状态表(✅/⚠️/❌ + 证据):
1. Shippo 运费报价对非加州地址(NY/TX 各一)能返回费率(测试模式,`/api/store/shipping-rates`)。
2. tax-rate 路由对外州地址的行为(收不收、怎么算)——只记录现状。
3. Stripe 当前密钥模式、webhook 配置、Resend 域名状态——只记录,**切换正式模式是 Eddie 的运营动作,不要执行**。
4. featured 商品(T2 完成后)逐个检查:主图、描述、配置项完整,能出运费。

---

## 回报格式(全部完成后一份)

- 每任务:改动文件清单 + commit hash + recon 发现(尤其 T2 字段名、T1 HD 页现状、T3 替换点列表)。
- T5:配置状态表。
- 与任务书不符/未做项:单独列出原因。
- 已知给 Sonnet 的测试入口:本地起服务命令、测试账号/开关位置。

失败回路:Sonnet 测出问题直接回你修;修不动时汇总现象+已尝试项回 Fable 重新诊断。
