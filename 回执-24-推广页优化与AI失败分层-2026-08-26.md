# 回执 — 整改 #24（推广页优化 + AI Bug）

日期：2026-08-26　执行：Opus
- `window-treatments` 提交 `c250be1`（#24-1 / #24-2 / #24-3）· 未推送
- `AAPP` 提交 `eeda120` 在分支 `fix/partner-sync-inline-badge`（合作方未同步徽章补内联区）· 未合并

---

## #24-1　「浏览产品」入口前置

`apps/web/src/app/r/[token]/ReferralLanding.tsx`

原来它是**整页最后一个 section** 里的一个灰色文字链接（`text-gray-600`），排在三张卡片和两个 CTA 之后。想先看看我们卖什么的客户，得一路划到底。

现在紧跟在公司介绍那句话下面，做成描边按钮（48px 触控目标 + 箭头 + 一句「先看看我们做什么，再动手量窗」）。主路径就是你要的顺序：

> 公司介绍 → **浏览产品** → 量窗 → 问 AI → 免费上门量窗

页尾那个重复的文字链接删掉了 —— 同一个入口出现两次，上面那个就不像主路径了。

**一处我自己拿的主意，你可以推翻**：这个按钮用的是**描边**不是实心。页面上唯一的实心深色按钮我留给了「问 AI 顾问」——那才是真正出线索的一步。要是你觉得浏览产品该更抢眼，我改成实心，两分钟的事。

## #24-2　合作方页拆两层

你选了两层页面，做完了。

| | 路径 | 内容 |
|---|---|---|
| 第一层 | `/partner/{token}` | QR + 分享链接 + 推荐码 + 复制/分享/短信按钮 + 一句说明。**能直接把手机递给客户。** |
| 第二层 | `/partner/{token}/details` | 推荐数、成交数、W-9 上传与状态、佣金答疑口径。Agent 自己点进去才看得到。 |

第一层留了一个入口「我的合作方信息 →」，**文案刻意是中性的**。这一点我写成了守卫直接禁掉 `W-9` 这个词出现在第一层：入口要是写成「⚠ 还差 W-9」，等于把刚搬走的那件事又贴回客户眼前，白拆。

其余保持原有规矩：两层都 `noindex`，两层都一个金额不显示，details 页同样卡 token 合法性与 partner 类型（不是只靠「知道链接」）。第二层顶部有一步返回分享页。

**守卫的形状值得说一句**：我没写「不许出现 W-9」这种一次性断言，而是维护了一张 `AGENT_ONLY` 清单（推荐计数 / 成交计数 / W-9 标题 / W-9 上传接口 / 佣金两句话……），每一项都必须**在第二层出现、在第一层不出现**。以后谁加一块新的 Agent 专属信息，把标记加进清单，守卫就替他盯着加错页这件事。

## #24-3　AI 回答失败

**先说清楚：我没有替你诊断出那一次到底坏在哪。** 你的截图上只有那句统一文案，日志我这边看不到，凭现有信息判断不了是模型、API 还是我们自己的函数 —— 这正是你说的那个问题。所以这一单做的是**让下一次可诊断**。

查代码发现的实情：`/api/store/assistant` 有**四个**失败出口，共用同一句 `The assistant is having trouble right now`，日志里也只有一句 `console.error`。事后完全对不上号。

改成分层：

| 码 | 什么情况 |
|---|---|
| `AI_TIMEOUT` | 上游没在时限内回来（我们自己 abort 的，或 408/504） |
| `API_RATE_LIMIT` | 429，或响应体明说 rate limit |
| `API_OVERLOADED` | 529 / 503 / 明说 overloaded |
| `API_AUTH` | 401 / 403 —— key 没配对或被撤 |
| `API_ERROR` | 其它非 2xx，归不了类的（响应体前 200 字进日志） |
| `TOOL_LOOP_EXHAUSTED` | 工具来回 5 轮还没给出最终答复 |
| `INVALID_RESPONSE` | 上游 200 了，但内容为空 / 只剩一行快捷回复 |
| `FUNCTION_ERROR` | 我们自己这一层抛了异常 |
| `QUOTE_TOOL_ERROR` | 定价/报价类工具失败（单独一类——它坏了客户拿到的就是错的价） |

- 日志：`[assistant] FAIL <CODE> {upstreamStatus, iter, turns, detail}`。只放排查用的元信息，不放对话内容、客户姓名电话、key。
- 前端 payload 带 `code`。
- **客户看到的那句话末尾附 `(Ref: AI_TIMEOUT)` 这样一个参考码** —— 你下次再截图，看一眼就知道该查哪一层。这条是冲着你的实际工作方式做的：你是拿截图来问的。

**顺带发现的一个真问题**：那个 fetch **以前没有任何超时**。上游一挂起，就一路挂到平台把函数掐掉，客户端拿到的是裸网络错误，我们这边连一行日志都没有。现在自己关闸（25s），挂起变成一个干净的 `AI_TIMEOUT`。**如果你那次的截图对应的就是这种情况，以前是不可能查出来的。**

还有一处容易埋错方向的地方：兜底 `catch` 现在会先分类，`AbortError` 归 `AI_TIMEOUT`。把它算成 `FUNCTION_ERROR`，等于把「上游太慢」误报成「我们的代码有 bug」，下次照样查错方向。

## 守卫与变异验证

新增两个检查，都接进了 npm：

```bash
npm run check:assistant   # 29 passed（分类器是真跑起来的，不是文本断言）
npm run check:partner     # 33 passed
npm run check:colors      # 原有的，仍绿
npx tsc --noEmit          # 干净
```

10 条变异全部确认变红再还原（429 不认限流 / 文案不带参考码 / 声明一个没人产出的码 / fetch 不挂超时闸 / catch 一律算我们的 bug / W-9 贴回第一层 / 推荐数加回第一层 / 浏览产品入口挪走 / details 丢 noindex / 第二层回不去分享页）。

**★ 有一条第一次没红，记一笔。** 我原来只用一个样本测超时分类：`name='AbortError'` 且 `message='The operation was aborted'`。这个样本**两条分支同时命中**，所以我把 name 分支写死成 `false`，测试照样全绿 —— 断言看着有，实际没在守。拆成两个样本（只有 name / 只有 message）之后才真红。

「一个样本同时满足两个分支」是这类假绿的典型形状，和固定长度窗口是同一族的东西。

## 没做 / 没跑

- `next build` 在这台机器上跑不完（历史问题），**没有假称通过**。tsc 是干净的。
- #24-3 的**根因**仍未定位（见上）。下次复现时，把气泡里那个 `(Ref: XXX)` 发我，或者从 Vercel 日志里 grep `[assistant] FAIL`，我就能直接从那一层往下查。

## 部署

```bash
cd ~/…/window-treatments
git push origin main          # Vercel 自动发布
```

后端函数不用单独部署（Next.js route 随站点一起走）。

## 另一件事：AAPP 那边的分支还没合

上一单的 v1010（合作方「未同步」徽章补客户档案内联区）我提交在分支上，没落 main —— **主工作区的索引里躺着一份 452 行的 v1008 回滚**（删掉 QNOTE 产品备注、`scripts/quote-note-tests.js`、`docs/gpt-knowledge-sync-2026-08-25.md`，以及 CLAUDE.md 里那条「更新日志」规则），不是我放的。直接在主工作区提交会把那份回滚一起带走。

```bash
cd ~/…/AAPP
git status                                  # 先自己确认那份暂存要不要
git restore --staged .                       # 确认是陈旧的就清掉
git merge --ff-only fix/partner-sync-inline-badge
```

顺带：AAPP 主工作区的 `sw.js` / `index.html` 还停在 **v1004**，而 HEAD 已经 v1009 —— 工作区整体落后 HEAD 很多，那份陈旧暂存八成就是这么来的。
